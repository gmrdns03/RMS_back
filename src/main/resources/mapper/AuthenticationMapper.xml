<!-- AuthenticationMapper.xml -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.LimeRMS.mapper.AuthenticationMapper">
  <insert id="insertAuthenticationTest" parameterType="com.project.LimeRMS.entity.Authentication">
    INSERT INTO Authentication (authNm, authDesc, priority, regUserId, modfUserId)
    VALUES (#{authNm}, #{authDesc}, #{priority}, #{regUserId}, #{modfUserId})
  </insert>
  <select id="findByRegUserId" parameterType="java.lang.String" resultType="com.project.LimeRMS.entity.Authentication">
    SELECT * FROM Authentication WHERE regUserId = #{regUserId}
  </select>
  <select id="findAuthNmByAuthId" parameterType="java.lang.Integer" resultType="java.lang.String">
    SELECT authNm FROM Authentication WHERE authId = #{authId}
  </select>
</mapper>
